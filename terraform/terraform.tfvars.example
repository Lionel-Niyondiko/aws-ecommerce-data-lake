# Copy to terraform.tfvars and fill in. Never commit terraform.tfvars.

budget_alert_email = "you@example.com"

# aws_region       = "us-east-1"
# budget_limit_usd = 20

# In CI, set this to the GitHub OIDC role ARN so the pipeline role trusts it.
# trusted_principal_arn = "arn:aws:iam::123456789012:role/lab-datalake-github-actions"
